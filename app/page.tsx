import { LandingPage } from "@/components/landing/LandingPage";

/** Bosh sahifa — ommaviy landing. Panel: /panel (login talab qiladi). */
export default function HomePage() {
  return <LandingPage />;
}
